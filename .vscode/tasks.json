{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Processing Sketch",
            "type": "shell",
            "command": "processing-java",
            "args": [
                "--force",
                "--sketch=${workspaceFolder}\\main",
                "--output=${workspaceFolder}\\out",
                "--run"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
